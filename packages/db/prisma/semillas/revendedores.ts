import type { PrismaClient } from '../../src/index.js';

/** Niveles, precios mayoristas y el revendedor de demostración (fase 2). */
export async function sembrarRevendedores(_prisma: PrismaClient): Promise<void> {}
